import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lidz.ignites.me',
  appName: 'Lidz: Instant Answers, Seamless Updates',
  webDir: 'dist/mobile-app/browser'
};

export default config;
